// C Program to Determine the Unicode Code Point at a Given Index
#include <stdio.h>

int main() {
    char str[] = "Hello";
    int index = 1;
    printf("Unicode at index %d: %d\n", index, str[index]);
    return 0;
}
